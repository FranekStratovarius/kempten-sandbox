/**
	Schreiben Sie eine Funktion mit der Deklaration *** die zwei 
	Bitblöcke in dem gegebenen Wert value wie folgt gegeneinander 
	austauscht. Zunächst wird durch die Parameter fromBit und toBit eine 
	Region von Bits in value abgegrenzt. Die Funktion soll nur auf diesem 
	Bitbereich arbeiten und alle anderen Bits unangetastet lassen.
*/
unsigned int exchangeHalves(unsigned int value, int fromBit, int toBit);

/**
	Schreiben Sie eine Funktion mit der Deklaration ***, die einen Wert 
	zurückgibt, in dessen Binärdarstellung die Bits gerade anders herum 
	auftauchen als im gegebenen b.
*/
unsigned int reverse(unsigned int b);
